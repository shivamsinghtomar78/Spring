server:
  port: 8085    # or any other available port

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/Job?createDatabaseIfNotExist=true
    username: root
    password: 0819
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
    ddl-auto: update
    database: mysql
    show-sql: true